# Destination for shortcuts
DEST_DIR="${HOME}/Desktop"

# Log file path
LOG_FILE="${HOME}/.local/share/desktop-shortcut-watcher.log"

# Overwrite existing .desktop files: "true" or "false"
OVERWRITE="true"

# Regex to skip certain apps, e.g. "^(org.gnome|com.canonical)"
BLACKLIST_REGEX=""

# Include Terminal=true apps as desktop icons: "true" or "false"
INCLUDE_TERMINAL="true"
